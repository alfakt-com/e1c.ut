
Процедура ОсновныеДействияФормыУстановить(Кнопка)
	Константы.infor_СтрокаПодключения.Установить(СтрокаПодключения);
КонецПроцедуры

Процедура ПриОткрытии()
	СтрокаПодключения = Константы.infor_СтрокаПодключения.Получить();
КонецПроцедуры

Процедура ОсновныеДействияФормыПроверить(Кнопка)
	СтрокаПодключения = Константы.infor_СтрокаПодключения.Получить();
	Попытка
		Connection = Новый COMОбъект("ADODB.Connection");
		Connection.Open(СокрЛП(СтрокаПодключения));	
		Connection.Close();
		Сообщить("Ок");
	Исключение
	    Сообщить(ОписаниеОшибки());
	КонецПопытки;
КонецПроцедуры
