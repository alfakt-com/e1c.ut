
&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	ЭтаФорма.ТолькоПросмотр = Истина;
	ВалютаУУ = Константы.ВалютаУправленческогоУчета.Получить();
КонецПроцедуры

&НаКлиенте
Процедура НаименованиеНажатие(Элемент, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	Поступление = swОбщегоНазначения.ПолучитьДокументИзСвязейДокументовИПартий(Объект.Ссылка);
	Если ЗначениеЗаполнено(Поступление) Тогда
		ОткрытьЗначение(Поступление);
	КонецЕсли;
КонецПроцедуры
