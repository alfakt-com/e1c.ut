&НаКлиенте
Процедура ОбработкаКоманды(ПараметрКоманды, ПараметрыВыполненияКоманды)
	
	Если ЗначениеЗаполнено(ПараметрКоманды) Тогда
		
		ДокументОснование = ПараметрКоманды[0];	
		ПараметрКоманды = Новый Структура();
		ПараметрКоманды.Вставить("ДокументСсылка",  ДокументОснование);
		ПараметрКоманды.Вставить("ЭтоВводОстатковИзВходящегоЭСФ", Истина);
		
		ВСКлиент.ВыполнитьКомандуСоздатьЭДВС(ПараметрКоманды);
		
	КонецЕсли;
	
КонецПроцедуры
